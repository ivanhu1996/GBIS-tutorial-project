extends TextureRect

func _ready():
	#var shader = Shader.new()
	#shader.code = """

	#var mat = ShaderMaterial.new()
	#mat.shader = shader
	#mat.set_shader_parameter("tex_size",texture.atlas.get_size())
	#mat.set_shader_parameter("region_pos",texture.atlas.region.position)
	#mat.set_shader_parameter("region_size",texture.atlas.region.size)
	pass
